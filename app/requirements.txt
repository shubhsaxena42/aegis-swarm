# ═══════════════════════════════════════════════════════════════
#  Python Dependencies — Aegis Swarm Infrastructure
# ═══════════════════════════════════════════════════════════════

# ── Redpanda / Kafka ──────────────────────────────────────────
confluent-kafka>=2.6.0

# ── Prometheus ────────────────────────────────────────────────
prometheus-client>=0.21.0

# ── MLFlow ────────────────────────────────────────────────────
mlflow>=2.19.0
boto3>=1.35.0            # S3/MinIO artifact storage
psycopg2-binary>=2.9.0   # PostgreSQL backend

# ── DVC ───────────────────────────────────────────────────────
dvc[s3]>=3.56.0

# ── Apache Flink (PyFlink) ────────────────────────────────────
apache-flink>=1.20.0

# ── AI / ML / Ray Serve (YOLO Inference) ──────────────────────
ray[serve]>=2.9.0        # Ray Serve for model serving
ultralytics>=8.1.0       # YOLOv8/v26
boxmot>=0.7.0            # BoT-SORT Tracking
numpy>=1.26.0
opencv-python-headless>=4.9.0  # Headless for server env
torch>=2.1.0             # PyTorch

# ── Utilities ─────────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0.0
flask>=3.0.0             # For simple health check servers
gunicorn>=21.2.0
